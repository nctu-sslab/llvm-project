# This pass will be compiled into llvm default workflow of nvptx target
# A llvm module is also compiled for fast testing



# Commnad

opt -S -load $LLVM_BUILD_PATH/lib/LLVMOmpTgtAddrTrans.so -ompat -o out.ll < 2D.cPreOmpATPass.ll
